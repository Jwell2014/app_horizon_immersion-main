<form method="post" action="{{ path('app_img_sat_delete', {'id': img_sat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ img_sat.id) }}">
    <button class="btn">Delete</button>
</form>
